# 🥂 BenchTalks-API

Современный Backend для приложения знакомств, который не просто сводит людей, но и помогает им найти идеальное место для первой встречи.

## ✨ Основные фишки

* **Smart Bench Finder**: Автоматический поиск ближайших скамеек между двумя пользователями через **OpenStreetMap (Overpass API)**.
* **Fairness Scoring**: Алгоритм ранжирования мест для встреч на основе "справедливости" (чтобы обоим было одинаково удобно добираться).
* **Discovery Engine**: Умная лента профилей с фильтрацией по геолокации, полу и возрасту.
* **Real-time Logic**: Система свайпов, мгновенное создание мэтчей и чаты.

---

## 🚀 Технологический стек

* **Framework:** FastAPI (Python 3.13)
* **Database:** PostgreSQL + SQLAlchemy 2.0
* **Validation:** Pydantic v2
* **Data Source:** OpenStreetMap (Overpass API)
* **Containerization:** Docker + Docker Compose

---

## 🛠 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/sani4forever/BenchTalk-API.git

```

### 2. Запуск через Docker

Приложение полностью контейнеризировано. Просто запустите:

```bash
docker-compose up --build

```

API будет доступно по адресу: `http://localhost:8000/api/v1/docs#/`

---

## 📍 API Эндпоинты (Ключевые)

### Поиск мест для встреч (Benches)

| Метод | Эндпоинт | Описание |
| --- | --- | --- |
| `POST` | `/api/v1/matches/{id}/suggest-benches` | Найти скамейки для участников мэтча |
| `GET` | `/api/v1/benches/search` | Свободный поиск скамеек по координатам |
| `POST` | `/api/v1/matches/{id}/benches/{bid}/accept` | Подтвердить выбор места встречи |

### Пользователи и Свайпы

* `POST /api/v1/users` — Регистрация профиля.
* `GET /api/v1/users/{id}/discover` — Получение ленты кандидатов.
* `POST /api/v1/users/{id}/swipe` — Лайк/Дизлайк (создает мэтч при взаимности).

---

## 🧠 Как работает алгоритм "Скамейка"

Алгоритм поиска (метод **CIRCLE**) работает в три этапа:

1. **Search Zone**: Вычисляется средняя точка между пользователями и радиус поиска ().
2. **OSM Query**: Запрос в Overpass API для поиска всех объектов `amenity=bench` в этой зоне.
3. **Scoring**: Каждая скамейка получает балл по формуле:



*Где  — расстояния от пользователей до скамейки. Чем ниже балл, тем выше скамейка в списке.*

---

## 📂 Структура проекта

```text
├── src/
│   ├── api_versions/
│   │   └── v1/
│   │       ├── bench_finder.py  # Логика OpenStreetMap
│   │       ├── crud.py          # Операции с БД
│   │       ├── models.py        # SQLAlchemy модели
│   │       ├── schemas.py       # Pydantic модели
│   │       └── routes.py        # API Роуты
├── start_app.py                 # Точка входа
└── docker-compose.yaml

```

---

## 📝 Лицензия

Distributed under the MIT License. See `LICENSE` for more information.

---
